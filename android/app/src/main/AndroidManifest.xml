<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SMS permission with clear purpose for transaction tracking -->
    <uses-permission android:name="android.permission.READ_SMS" 
        android:maxSdkVersion="33"
        tools:node="replace" />
    <uses-permission-sdk-23 android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Only request MANAGE_EXTERNAL_STORAGE for Android 11+ and only when needed -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" 
                     tools:ignore="ScopedStorage" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:usesCleartextTraffic="false"
      android:networkSecurityConfig="@xml/network_security_config"
      tools:targetApi="24"
      tools:replace="android:usesCleartextTraffic">

      <!-- Privacy metadata for Play Store -->
      <meta-data
          android:name="payment_tracker_privacy_compliant"
          android:value="true" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- SpendBook Full Spending Widget -->
      <receiver
        android:name=".widget.SpendingWidgetProvider"
        android:label="SpendBook Spending Widget"
        android:exported="true">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/spending_widget_info" />
      </receiver>

      <!-- SpendBook Compact Widget -->
      <receiver
        android:name=".widget.CompactSpendingWidgetProvider"
        android:label="SpendBook Compact Widget"
        android:exported="true">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/spending_widget_small_info" />
      </receiver>
    </application>
</manifest>
